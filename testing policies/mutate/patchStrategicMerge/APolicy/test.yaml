name: add-nodeselector
policies:
  - policy.yaml
resources:
  - resource.yaml
results:
  - policy: add-nodeselector
    rule: add-nodeselector
    resource: myapp-pod
    patchedResource: patch.yaml
    kind: Pod
    namespace: practice
    status: pass