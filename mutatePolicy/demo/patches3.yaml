apiVersion: v1
kind: Pod
metadata:
  labels:
    foo: bar
  name: myapp-pod2
  namespace: practice
spec:
  containers:
  - image: nginx:latest
    name: nginx
  dnsConfig:
    options:
    - name: ndots
      value: "2"