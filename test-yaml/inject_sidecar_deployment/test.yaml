name: inject-sidecar
policies:
  - inject_sidecar_deployment.yaml
resources:
  - resource.yaml
results:
  - policy: inject-sidecar
    rule: inject-sidecar
    resource: mydeploy
    patchedResource: patch.yaml
    kind: Deployment
    namespace: default
    status: pass
