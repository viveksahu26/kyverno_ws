name: add-volume
policies:
  - add_volume_deployment.yaml
resources:
  - resource.yaml
variables: values.yaml
results:
  - policy: add-volume
    rule: add-volume
    resource: mydeploy
    patchedResource: patch.yaml
    kind: Deployment
    namespace: default
    status: pass
